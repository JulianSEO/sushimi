import React from "react";

import { StyledFooter } from "./styles";

const Footer = () => {
  return (
    <React.Fragment>
      <StyledFooter />
    </React.Fragment>
  );
};

export default Footer;
